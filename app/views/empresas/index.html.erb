<br>
<div class="d-flex flex-row-reverse bd-highlight">
  <%= button_to 'Sair', destroy_user_session_path, method: :delete, class:"btn btn-danger" %>
  &nbsp;
  <%= button_to 'Perfil', , method: :delete, class:"btn btn-info" %>
</div>
<br><h2> Bem-vindo, <%= current_user.email %>! </h2><br>

<h3>Empresas</h3>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @empresas.each do |empresa| %>
      <tr>
        <td><%= empresa.nome %></td>
        <td><%= empresa.email %></td>
        <td><%= link_to 'Ver', empresa_path(empresa), class:"btn btn-primary" %></td>
        <td><%# fazer aparecer o alert, ainda não tá funcionando %>
          <%= button_to 'Apagar', empresa, method: :delete,
            class:'btn btn-danger',
            data: { confirm:'Tem certeza disso?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Criar nova empresa', new_empresa_path, class:"btn btn-primary" %>